# frontend2

Flutter client scaffold for the OpenID Connect server in `backend2`.

Run with `flutter run`. Configure the OIDC discovery URL as `http://127.0.0.1:8001/oidc/.well-known/openid-configuration` when adding the login flow.
